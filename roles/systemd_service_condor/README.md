# systemd_service_condor

Installs condor as a service.
## Variables
This role does not have any variables.


## Example usage in a playbook

```yaml
- hosts: [myserver]
  roles:
    - role: jamesread.soe.systemd_service_condor
```
